---
tags:
  - note
aliases:
  - Self-supervised learning
title: self-supervised learning
description:
parent nodes:
  - "[[deep learning]]"
child nodes:
annotation-target:
published on:
---

- **Self-supervised learning (SSL)** is a form of machine learning where <mark style="background: #BBFABBA6;">the model trains itself by creating its own labels from "unlabelled" raw data</mark>
- It effectively turns an unsupervised learning problem into a supervised one by hiding parts of the input and tasking the model with predicting those missing pieces