---
tags:
  - code
aliases:
  - heapq
title: python heapq
description:
parent nodes:
  - "[[202512261842 - python]]"
published on:
---

1. [heapq](https://www.geeksforgeeks.org/python/heap-queue-or-heapq-in-python/)
	1. `heapq.heapify(x)`
		1. in-place
	2. `heapq.heappush(heap, item)`
	3. `heapq.heappop(heap)`
	4. `heappushpop(heap, item)`
		1. push then pop
	5. `heapreplace(heap, item)`
		1. pop then push
	6. `nlargest(n, iterable, key=Optional)`
	7. `nsmallest(n, iterable, key=Optional)`
	8. `merge(heap1, heap2)`